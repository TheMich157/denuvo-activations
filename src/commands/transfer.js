// Transfer command - points system removed, this is a placeholder to prevent import errors

export async function handleButton(interaction) {
  // Points transfer system was removed
  // This function exists to prevent import errors
  return false;
}
